# IDE Adapters

This directory holds IDE-specific consumption notes for the canonical team assets.

The assumption is that IDE environments are thinner consumers than agent runtimes, so V1 should prefer:

- reference files
- prompt bundles
- lightweight workspace bootstraps

Keep IDE-specific packaging here rather than pushing it into the canonical assets.
