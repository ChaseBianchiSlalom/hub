#!/usr/bin/env bash

set -euo pipefail

repo_root="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
exec env ADOBE_AI_HUB_ROOT="$repo_root" node --experimental-strip-types "$repo_root/src/cli.ts" "$@"
